#include <iostream>

using namespace std;

//template <typename T>
class TestBinTree {
private:
	float value;
public:
	void testBinTree();
	float calculat(float value) {
		//return this.val(right.val,left.val);
		return value;
		}
	//float calculate(const BinTree<T>& bintree);
};